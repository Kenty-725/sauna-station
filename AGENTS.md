# AGENTS.md

このファイルはリポジトリ全体に対する共通ガイドです。詳細ルールは各ディレクトリ直下の `AGENTS.md` を優先してください。

## 対象範囲

- このファイル: リポジトリ全体
- `frontend/AGENTS.md`: フロントエンド実装
- `api/AGENTS.md`: バックエンド実装

作業対象が `frontend/` または `api/` 配下にある場合は、最も近い `AGENTS.md` を優先して従ってください。

## プロジェクト概要

- プロダクト: サウナ施設の検索・予約システム
- フロントエンド: React 19 + TypeScript + Vite
- バックエンド: Ruby on Rails API + MySQL
- 開発環境: Docker Compose

## 共通方針

- 変更は依頼範囲に限定し、無関係なリファクタは混ぜない
- 既存の命名規則、ディレクトリ構成、実装パターンを優先する
- 影響範囲がある変更では、実装とあわせて最低限の検証手順を示す
- 生成コードを追加する場合も、そのままコミットせず内容を確認する
- 環境変数を追加・変更した場合は `.env.example` と README の更新要否を確認する

## 設計の共通原則

- 一時的に動く実装より、意図が読めて変更しやすい構造を優先する
- 責務の境界を明確にし、UI、通信、ドメインルール、永続化を安易に混ぜない
- 抽象化は問題が見えた後に行い、早すぎる共通化は避ける
- ただし重複が構造的になった場合は、放置せず責務単位で整理する
- エラー処理、認証、入力検証は正常系と同等に設計対象として扱う

## 作業前チェック

- まず関連ファイルと既存実装を確認してから変更する
- UI 変更は画面単位、API 変更はエンドポイント単位で影響範囲を確認する
- 既存テストがある領域では、同じ粒度でテスト追加を検討する

## 禁止事項

- 秘密情報、認証情報、実データをコミットしない
- ユーザーが作成した未確認の差分を勝手に巻き戻さない
- 大きな構成変更を、依頼なく横断的に進めない

## 推奨コマンド

- 全体起動: `docker-compose up`
- フロント: `docker-compose up frontend`
- API: `docker-compose up api`
- フロントのコマンド実行: `docker-compose exec frontend sh`
- API のコマンド実行: `docker-compose exec api bash`

## レビュー観点

- 変更が既存仕様を壊していないか
- 責務分離が崩れていないか
- 命名と配置が既存ルールに沿っているか
- テストまたは検証手順が不足していないか
